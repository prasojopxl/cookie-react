import styled from "styled-components";
import { gap } from "../../assets/css";

export const ConntainerPartner = styled.div `
    padding: ${gap} 0;    
`

export const ContainerListBrand = styled.div `
	display: flex;
	align-items: center;
    justify-content: space-evenly;
    
`